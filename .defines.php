<?php

if ( ! defined( 'DS' ) ) {
    define( 'DS', DIRECTORY_SEPARATOR );
}
if ( ! defined( 'SAU_EXTEND_PATH_LOADER' ) ) {
    define( 'SAU_EXTEND_PATH_LOADER', WPMU_PLUGIN_DIR . DS . 'a.sau.ext.php' );
}
if ( ! defined( 'SAU_EXTEND_PATH' ) ) {
    define( 'SAU_EXTEND_PATH', __DIR__);
}
